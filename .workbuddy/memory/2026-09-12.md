# 2026-09-12

- EduCore 系统通读完成（全程只读，零文件改动）。全貌：monorepo（pnpm 9 + turbo）= `apps/web`（React 19 + Vite，IndexedDB 离线层 + syncQueue 重放）+ `apps/api`（Fastify 5 + Mongoose 8 + Redis 可降级）+ `packages/algorithms`（BKT/IRT/SM-2/选题纯函数）+ `modules/`（6 个内容模块，moduleLoader 启动时 zod 校验并 upsert 进 Mongo）。
- 本轮补读：`auth.service.ts`（scrypt 密码哈希 + jose HS256 JWT + Redis refresh token 轮换、Redis 不可用时降级 + profile 更新字段白名单）、`apps/web/src/api/client.ts`（401 单飞静默刷新 + 温暖文案错误 toast）。`sync.service.ts` 后端不存在，离线同步逻辑在前端 `db/sync-queue.ts`。
- Owner 约束：所有阅读/改动仅限 `D:\Development\educore` 内；目录外任何操作需 Owner 在对话内明示批准。
- 治理状态：M84–M86 已接受；M87 Developer 100% / QA 0%，待签署 `QA_M87_ACCEPTANCE_2026-09-09.md`；M88–M92（Pilot Core）已规划未开票，M88 必须等 M87 签署后启动。
- 与 Owner 对齐功能认知：以 `docs/PROJECT_BOARD.md`「功能真相板」为权威——Implemented*（学习主链/BKT/错题/教师只读）、Partial（登录/IRT·SM-2/离线队列/内容模块）、Simulated-testable（AI 降级链/弱网 profile）、Placeholder·Non-Goal（classes/家长/志愿者/支付/真实 Pi）。开发策略 = 先把下层证据化（M88 Runtime Truth），不新增功能面。待 Owner 确认 M87 QA 签署方式与下一步方向。
