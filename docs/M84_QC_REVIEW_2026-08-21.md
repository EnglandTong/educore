# M84 QC Review — 2026-08-21

角色：Controller / Developer / QA（Owner 授权同一 Agent 兼任）  
状态：`Needs Fix — partial acceptance evidence`

## Passed evidence

- API typecheck pass.
- Web typecheck pass.
- API tests: 11 files / 54 tests pass.
- Web production build: Vite 2818 modules and PWA assets generated.
- Isolated real HTTP: login, session start, question, grading, training end and cross-account
  rejection pass.
- Same answer `eventId` replay returns the same completed result without incrementing attempts.
- Sync batch records completion and `/sync/status` returns a real `lastSyncedAt`.
- System Chrome: login → dashboard → training → question → answer → next question pass.
- System Chrome offline mode: answer retained while offline and replayed after reconnect; UI
  advanced to the next question.

## Remaining M84 gates

- Direct IndexedDB evidence for successful queue deletion and retry-limit exhaustion is not yet
  captured.
- `end_session` now has a server-side `SyncEvent` receipt and duplicate replay evidence; direct
  IndexedDB deletion/retry-limit exhaustion is still not captured.
- BKT `pKnown` persistence and derived read-model invariants have a direct database assertion:
  `pKnown=0.5263157894736843`, `score=72`, `level=proficient`, one attempt.
- Full seven-chain security matrix remains outside the M84 student-loop slice.

## QA decision

M84 is not yet Accepted. Continue bounded remediation until R1–R6 have direct evidence, then
perform a final same-role QA decision. M85 and M86 remain frozen.
