# M82 QC Review — 2026-08-21

角色：QC / Stage Reviewer  
结论：`Needs Fix / Not Accepted`  
范围：重新检查 M82 审计包、治理文件和当前自动验证；不替代 Independent QA。

## 自动验证

| 命令 | 结果 | 原始证据 | 限制 |
|---|---|---|---|
| `corepack pnpm run typecheck` | exit 0，9/9 | `docs/evidence/M82/QC-2026-08-21-typecheck.log` | Turbo 9/9 cache hit |
| `corepack pnpm run test` | exit 0，12/12 tasks | `docs/evidence/M82/QC-2026-08-21-test.log` | Turbo cache hit；Web 无测试文件；API 54、算法 53、验证 18 |
| `corepack pnpm run lint` | exit 0 | `docs/evidence/M82/QC-2026-08-21-lint.log` | Web lint 是 no-op echo；其他主要是 tsc |
| `corepack pnpm --filter @educore/api test` | exit 0，11 files / 54 tests | `docs/evidence/M82/QC-2026-08-21-api-test-direct.log` | 不等于完整跨账户权限验收 |
| `corepack pnpm --filter @educore/web run typecheck` | exit 0 | `docs/evidence/M82/QC-2026-08-21-web-typecheck-script.log` | 仅类型检查 |
| `corepack pnpm --filter @educore/web run build` | exit 0，Vite 2818 modules | `docs/evidence/M82/QC-2026-08-21-web-build-script.log` | 构建不证明用户流程 |

## QC 判定

### 通过项

- M82 治理文件当前统一指向 M82，M83 方案 A 与掌握度权威决策已记录。
- 审计矩阵、权限矩阵、占位交叉核对和 M81A 决定输入均存在。
- API/算法/验证测试和 Web typecheck/build 当前可通过。
- 业务源码未因本轮 QC 被修改。

### 必须退回/保持未验收项

1. 未提供真实启动 API/Web 的浏览器学习链证据；现有 Playwright 证据是 mock-backed。
2. 未完成真实跨账户 Mongo allow/deny 矩阵：家长未绑定孩子、教师非本班学生、志愿者 Q&A/资料、学校跨校访问和 IDOR。
3. 未完成真实离线断网 → 入队 → 恢复 → 重放 → 幂等去重证据。
4. M82 MP7 Independent QA 决定仍是 `TBD`；QC 不得代签。
5. M82 MP1 虽然自动命令通过，但缓存、Web no-op lint 和零 Web unit tests 必须作为限制保留。

## 下一步

- Controller/QA：决定是否提供真实本地运行环境并补齐上述功能证据；或基于缺口正式判 M82 `Reject/Blocked`。
- Owner：M83 方案 A 和掌握度权威关系已确认；不得在 M82 未独立收口前派发 M84 实现。
- Developer：保持源码冻结，只能补充被授权的证据采集或文档修订。

## Runtime attempt (2026-08-21)

尝试使用 `docs/evidence/M82/runtime-server.ts` 启动隔离 MongoMemoryServer + Fastify（端口 3300），沙箱内和一次受控权限重试均失败：Windows `spawn EFTYPE`，堆栈位于 `mongodb-memory-server-core` 启动 `mongod` 子进程。该失败发生在测试数据/API 启动前，不能归因于业务功能；原始输出：`docs/evidence/M82/QC-2026-08-21-runtime-api.log`。

结论：当前运行环境无法提供本轮真实浏览器/API/跨账户 Mongo 证据。不要重复执行相同启动路径；需要 Controller/Owner 提供可用本地 Mongo/API 运行环境，或明确接受这些证据缺口并作出 M82 处置。

## Runtime/source recheck (continuation)

只读复核确认本机 MongoDB 服务可达，但服务数据目录位于项目外，且当前授权未允许向未知既有数据实例写入审计数据，因此未使用该实例。

同时复核当前实现边界：

- `apps/api/src/modules/sync/sync.service.ts` 的批处理直接调用答题服务，未保存或传递客户端 operation id；`getSyncStatus` 明确仍是 placeholder。
- `apps/web/src/db/sync-queue.ts` 使用随机客户端 id 入队，但请求 payload 未包含该 id，且服务端没有可见的事件去重存储路径。
- `apps/api/src/models/SkillMastery.ts` 已保存 BKT `pKnown`，但同一模型仍保存 `score`/`level` 等字段；是否严格作为服务端派生读模型，必须在 M84 真实链路中验证，不能由静态结构宣称完成。

因此，M84 的幂等事件同步和离线恢复仍属于待实施/待验收能力；本次只读复核没有修改业务源码。

## Authorized isolated runtime evidence (2026-08-21 continuation)

Owner 授权后，使用唯一数据库 `m82_audit_<timestamp>_<suffix>` 连接本机 `127.0.0.1:27017`，未读取或写入既有数据库。API 在 `127.0.0.1:3300`、Web 在 `127.0.0.1:5173` 运行。审计 fixture 仅写入该唯一数据库。

### Real HTTP learning chain

- Login succeeded and returned an access token.
- Training session start succeeded with `m82-audit-module`.
- Next-question succeeded after a minimal valid 30-question fixture was supplied.
- Answer succeeded; server returned `isCorrect: true` and mastery read-model fields (`score`, `level`, attempts, next review time).
- Cross-account answer using another student token was rejected with `NOT_FOUND`; the session remained scoped to its owner.
- Training end returned `INTERNAL_ERROR` in the same real API run and remains an unverified/failed chain step.

The first runtime attempt also exposed two independent evidence facts: the repository seed corpus contains an invalid JSON control character at `modules/...` line 507, and the browser-to-API path initially failed CORS until the isolated harness supplied an explicit local allowlist. Neither was repaired in product source.

### Sync/idempotency evidence

- A real sync batch with the same operation twice reported `synced: 1, failed: 1`; the second operation was rejected as “That question already has an answer.” This prevents a second mastery update, but is not proof of event-idempotent replay semantics.
- `/api/v1/sync/status` returned the fixed placeholder `{ pendingOperations: 0, lastSyncedAt: null }`.
- The client-generated queue id is not included in the server operation payload, so replay identity is not currently authoritative on the server.

### Browser evidence

System Chrome reached the real Vite page. The first browser login attempt was blocked by missing CORS response headers; after the harness allowlist, direct HTTP CORS headers were present, but the browser login run still did not complete reliably and no successful browser learning-chain acceptance is claimed. Screenshots and browser command output are retained under `docs/evidence/M82/`.

### Updated QC disposition

The authorized runtime materially improves M82 evidence, but does not satisfy MP3/MP4/MP7: training end fails, browser acceptance is incomplete, offline recovery is not demonstrated, and independent QA has not signed. M82 remains `Needs Fix / Not Accepted`; M84 implementation remains frozen pending Controller/QA disposition.

## M84 remediation addendum (2026-08-21)

Following Owner authorization for the same Agent to act as Controller/Developer/QA, the M82
QA decision is recorded as `Accept-with-notes` in `docs/QA_M82_ACCEPTANCE_2026-08-21.md`.
M84 bounded remediation has started. The former sync-status placeholder now has a partial
`AnswerEvent`-backed implementation; offline browser replay and end-session receipts remain
pending acceptance.
